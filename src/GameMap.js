class GameMap {
    constructor(macro) {
        console.log(macro);
        console.log(State);
    }

    run() {
        console.log("run ran. (gamemap)");

        return 'this is really just a widget??????????';
    }
}

export default GameMap;